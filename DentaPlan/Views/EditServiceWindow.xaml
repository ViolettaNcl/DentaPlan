<Window x:Class="DentaPlan.Views.EditServiceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактировать услугу" Height="255" Width="400" WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundColor}">
    <Grid Margin="20">
        <Grid.Effect>
            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.2"/>
        </Grid.Effect>
        <StackPanel>
            <TextBlock Text="Название услуги" Margin="0,0,0,5" FontSize="14"/>
            <TextBox x:Name="ServiceNameTextBox" Margin="0,0,0,10" FontSize="14"/>
            <TextBlock Text="Цена (руб)" Margin="0,0,0,5" FontSize="14"/>
            <TextBox x:Name="PriceTextBox" Margin="0,0,0,10" FontSize="14"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Сохранить" Click="SaveButton_Click" Width="100" Margin="0,0,10,0"/>
                <Button Content="Отмена" Click="CancelButton_Click" Width="100"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>